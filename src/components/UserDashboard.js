import React from "react";
import Comments from "./Comments"; // Adjust the import path if needed

const UserDashboard = () => {
  return (
    <div>
      <h1>Welcome to the Dashboard</h1>
      <Comments />
    </div>
  );
};

export default UserDashboard;
